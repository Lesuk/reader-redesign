<%= form_for(current_user.relationships.find_by(followed_id: @user), html: {method: :delete}, remote: true) do |f| %>
	<%= button_tag(type: 'submit', class: "btn btn-primary") do %>
		<%= set_icon("minus m-r-5", "fa") %>Unfollow
	<% end %>
<% end %>